export const metadata = {
  title: 'Contact',
  description: 'Get in touch for security engineering roles. Email, phone, LinkedIn, GitHub, resume download.',
}

export default function ContactLayout({ children }) {
  return children
}
